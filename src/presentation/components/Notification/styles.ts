import { styled } from '@stitches/react';

export const Container = styled('div', {
  position: 'absolute',
  top: '1.6rem',
  right: '1.6rem',
  padding: '1.6rem',
  backgroundColor: '#ccc',
  borderRadius: '0.4rem',
});
